<%= form_for(@notepad) do |f| %>
  <% if @notepad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notepad.errors.count, "error") %> prohibited this notepad from being saved:</h2>

      <ul>
      <% @notepad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :volunteers %><br />
    <%= f.text_field :volunteers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
